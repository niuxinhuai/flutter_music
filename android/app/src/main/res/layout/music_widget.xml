<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/music_widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/music_widget_background"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:paddingStart="14dp"
    android:paddingTop="10dp"
    android:paddingEnd="14dp"
    android:paddingBottom="10dp">

    <ImageView
        android:id="@+id/music_widget_cover"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:contentDescription="@string/music_widget_cover"
        android:src="@mipmap/ic_launcher" />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="10dp"
        android:layout_weight="1"
        android:orientation="vertical">

        <TextView
            android:id="@+id/music_widget_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:maxLines="1"
            android:text="Flutter Music"
            android:textColor="#202124"
            android:textSize="14sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/music_widget_artist"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="3dp"
            android:ellipsize="end"
            android:maxLines="1"
            android:text="点击播放当前歌曲"
            android:textColor="#7A7A7A"
            android:textSize="12sp" />
    </LinearLayout>

    <ImageView
        android:id="@+id/music_widget_previous"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:contentDescription="@string/music_widget_previous"
        android:padding="7dp"
        android:src="@android:drawable/ic_media_previous" />

    <ImageView
        android:id="@+id/music_widget_play_pause"
        android:layout_width="42dp"
        android:layout_height="42dp"
        android:contentDescription="@string/music_widget_play_pause"
        android:padding="6dp"
        android:src="@android:drawable/ic_media_play" />

    <ImageView
        android:id="@+id/music_widget_next"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:contentDescription="@string/music_widget_next"
        android:padding="7dp"
        android:src="@android:drawable/ic_media_next" />
</LinearLayout>
